<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.battman">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/bat"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/bateria"
        android:supportsRtl="true"
        android:theme="@style/Theme.Battery"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainPageActivity"
            android:label="@string/title_activity_main_page"
            android:theme="@style/Theme.Battery.NoActionBar"></activity>
        <activity android:name=".BatteryActivity"
            android:theme="@style/Theme.Battery.NoActionBar"/>
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.Battery.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>